@model WFTest2.Models.Process

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Process</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Process_Id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Process_Id)
            @Html.ValidationMessageFor(model => model.Process_Id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Form_Id, "Form")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Form_Id", String.Empty)
            @Html.ValidationMessageFor(model => model.Form_Id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Proces_Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Proces_Name)
            @Html.ValidationMessageFor(model => model.Proces_Name)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
